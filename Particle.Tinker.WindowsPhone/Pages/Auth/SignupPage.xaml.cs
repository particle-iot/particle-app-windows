using Windows.UI.Xaml.Controls;

namespace Particle.Tinker.Pages.Auth
{
    public sealed partial class SignupPage : UI.GenericPage
    {
        public SignupPage()
        {
            InitializeComponent();
            Init(RootGrid);
        }
    }
}
