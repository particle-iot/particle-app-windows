using Windows.UI.Xaml.Controls;

namespace Particle.Tinker.Pages.Auth
{
    public sealed partial class LoginPage : UI.GenericPage
    {
        public LoginPage()
        {
            InitializeComponent();
            Init(RootGrid);
        }
    }
}
