using Windows.UI.Xaml.Controls;

namespace Particle.Tinker.Pages.Auth
{
    public sealed partial class ForgotPasswordPage : UI.GenericPage
    {
        public ForgotPasswordPage()
        {
            InitializeComponent();
            Init(RootGrid);
        }
    }
}
